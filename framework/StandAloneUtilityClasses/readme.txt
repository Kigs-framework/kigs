The files in those folders are to be included directly in project, without other module initialisation.

You will need to declare them at application init before using by using the :

DECLARE_FULL_CLASS_INFO

macro.

